# Results directory

Generated experiment outputs are written here by default.

The directory is ignored by Git except for this README. To reproduce manuscript analyses, place released prediction tables under the paths documented in `data/README.md`, then run the scripts under `experiments/`.

